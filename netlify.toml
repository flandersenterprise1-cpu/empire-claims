# Netlify configuration (alternative free host to Vercel).
[build]
  command = "npm run build"
  publish = "dist"

# Single-page-app fallback: serve index.html for all client-side routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
